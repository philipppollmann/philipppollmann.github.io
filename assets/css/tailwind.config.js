const colors = require('tailwindcss/colors')

module.exports = {
    mode: 'jit',
    theme: {
        extend: {
            colors: {
                primary: '#202225',
                secondary: '#5865f2'
            }
        },
    },
    variants: {
        extend: {},
    },
    plugins: [],
}